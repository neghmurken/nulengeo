# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env          committed default values for the environment variables needed by the app
#  * .env.local    uncommitted file with local overrides
#
# DO NOT PUT REAL KEYS IN THIS FILE — use .env.local instead.

# MapTiler API key, restricted by domain in the MapTiler dashboard.
VITE_MAPTILER_KEY=
